{
  "name": "whossy-rules-tests",
  "private": true,
  "description": "Emulator tests for firestore.rules / storage.rules (pre-launch plan A6). Requires Java (Firestore/Storage emulators) — not runnable in every environment.",
  "scripts": {
    "test": "firebase emulators:exec --project whossy-app --only firestore,storage \"node --test rules.test.js\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4",
    "firebase": "^10.12.5"
  }
}
