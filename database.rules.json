{
  "rules": {
    ".read": false,
    ".write": false,
    "users": {
      "$uid": {
        "presence": {
          ".read": "auth != null",
          ".write": "auth != null && auth.uid === $uid"
        }
      }
    }
  }
}
